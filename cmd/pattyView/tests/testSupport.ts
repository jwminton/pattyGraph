import path from 'node:path'

export const fixture = path.join(import.meta.dirname, 'fixtures', 'schema4.jsonl')
export const bundleFixture = path.join(import.meta.dirname, 'fixtures', 'schema4.incident.zip')
